<UserControl x:Class="DesignPatterns.GUI_WPF.Views.AbstractFactoryUserControl"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DesignPatterns.GUI_WPF.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="CreateMotifScrollbarButton" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="200" />
            <ColumnDefinition MaxWidth="200" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition MaxHeight="50" />
            <RowDefinition MaxHeight="50" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Margin="20,20,0,0" Grid.Column="0" Grid.Row="0" Height="20" Width="140" Click="Button_Click_1">Create Motif Scrollbar</Button>
        <Button Margin="20,0,0,0" x:Name="CreatePmScrollbarButton" Grid.Column="0" Grid.Row="1" Height="20" Width="140" Click="Button_Click">Create PM Scrollbar</Button>
        <ScrollBar Name="CustomScrollbar" Grid.Column="1" Grid.RowSpan="3" Height="300" />
    </Grid>
</UserControl>
